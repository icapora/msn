## What changed

<!-- One or two sentences. -->

## Why

<!-- The problem this solves. -->

## Verification

<!-- What you ran, and what you saw. `npm run lint && npm test` at minimum. -->

- [ ] `npm run lint && npm test` pass
- [ ] If this touches how Claude Code data is read, `docs/compatibility.md` still holds
- [ ] If this changes the log record, `docs/log-format.md` is updated and `v` was considered
